#项目模块功能

## AI音乐引擎
### 生成器接口：TTAPI-suno
### 变量（模块化，可拓展）：
	#### MBTI：I/E，N/S，T/F，J/P滑块，调节MBTI组合以及各属性的比例
		功能：通过各属性的比例识别MBTI类型，从规则库中挑选对应prompt
		UI：对应色调，色彩组合：🟣 紫 NT，🟢 绿 NF，🔵 蓝 SJ，🟡 黄 SP，调整I/E的比例可以调整暗/亮比例，调整J/P可以调整色彩的细微变化（对应J和P的特质）
	#### 项目进程：根据项目场景切换不同的节奏和风格类型，比如 头脑风暴 /专注构思/代码冲刺/战鼓/我们落后了等场景
	#### 项目内容（可选）：用户可以关联项目文件夹，AI会扫描项目文件，分析readme等文件进行歌曲的风格补充
	#### 是否人声：用户可以选择是否有人声，如果有人声，则调用歌词生成模块生成歌词
	#### 歌词生成模块：根据MBTI，项目进程，项目内容等
	#### 风格选择（可选）：用户可以手动选择自己喜欢的曲风，比如citypop，Future Funk，K-PoP，8-bit，hiphop等，也可以留空
	#### 备注 （可选）：用户可以输入自己对音乐的需求，由AI进行分析和提示词补充
### 组装器：根据变量以及suno的文档，将变量转化为工程化提示词，发送到TTAPI接口进行音乐生成
### 接口：接受返回的输出结果，存到数据库，以备控制台调用

## 数据库
	### 元数据库，用于存储元数据信息
	### 生成端数据库，用于存储生成音乐用的数据
	### 音乐数据库，用于存储生成的音乐
## 控制台
	### 用于控制音乐编排，以及后期调整
## 前端UI
	### 用户操作界面

